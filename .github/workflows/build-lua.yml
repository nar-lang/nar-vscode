name: Build bundled Lua binaries

on:
  workflow_dispatch:
  push:
    paths:
      - "tools/build-lua.sh"
      - "tools/fetch-lua-windows.sh"
      - ".github/workflows/build-lua.yml"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: darwin-arm64
            runner: macos-14
          - target: darwin-x64
            runner: macos-13
          - target: linux-x64
            runner: ubuntu-latest
          - target: linux-arm64
            runner: ubuntu-24.04-arm

    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Build Lua for ${{ matrix.target }}
        run: tools/build-lua.sh ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lua-${{ matrix.target }}
          path: bin/${{ matrix.target }}/

  fetch-windows:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download Windows Lua from LuaBinaries
        run: tools/fetch-lua-windows.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lua-win32-x64
          path: bin/win32-x64/

  collect:
    needs: [build, fetch-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all platform artifacts
        uses: actions/download-artifact@v4
        with:
          path: bin/
          pattern: lua-*
          merge-multiple: false

      - name: Reshape into bin/<target>/ layout
        run: |
          set -e
          shopt -s nullglob
          for dir in bin/lua-*; do
            target="${dir#bin/lua-}"
            mkdir -p "bin/$target"
            mv "$dir"/* "bin/$target/"
            rmdir "$dir"
          done
          ls -la bin/*/

      - name: Upload combined bin/ archive
        uses: actions/upload-artifact@v4
        with:
          name: lua-bundle
          path: bin/
